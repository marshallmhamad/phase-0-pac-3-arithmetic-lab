function add(a, b) {
    return a + b;
      }
    function subtract(a, b) {
    return a - b;
    }
    function multiply(a, b) {
    return a * b;
    }
    function divide (a, b) {
    return a / b;
    }
    function increment(n) {
      return n += 1;
    }
    function decrement(n) {
      return n -= 1;
    }
    function makeInt(n) {
     return Number.parseInt(NaN)
    }
    function  makeInt(n) {
      return Number.parseInt(n)
    }
    function preserveDecimal(n) {
    return  Number.parseFloat(NaN)
    }
    function preserveDecimal(n) {
    return  Number.parseFloat(n)
    }
    function makeInt(n) {
     return parseInt(n, 10)
    }